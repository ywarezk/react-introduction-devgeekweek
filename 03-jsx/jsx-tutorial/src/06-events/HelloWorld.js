

// Array.prototype.map
// const somenewArray = somearray.map((originalValue) => transform(originalValue))
export function EventsExample() {
	/**
	 * gives detail about the event
	 * @param {Event} event 
	 */
	const handleClick = (event) => {
		
	}
	// onClick, onSubmit, onMouse*
	return (
		<button onClick={ handleClick }>
			click me
		</button>
	)
}

